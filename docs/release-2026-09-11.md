# Approved site release — 2026-09-11

All 18 redesigned routes and the global light/dark revision are owner-approved. The owner explicitly authorized final verification, commit, push and publication. The release comes from the isolated `codex/site-redesign-release` worktree, based on `1499ddb85177bee7a80c7924e80b3ea5a5cabb41`. Incomplete changes in the saved project folder are excluded and preserved.

The final audit checks every route, 448 links, 46 anchors and 68 media URLs, including full reachability from the homepage. Added a small Shop · Coming soon footer link to resolve the only discovery gap. Existing Stats and video facade suites pass, as do the homepage, overview, Bowling, Survivors, character, Lore and utility checks. Theme verification covers every route on desktop/mobile in both modes, 320px overflow, keyboard/persistence and stateful Stats behavior. The flat-background contrast audit passes 2,734 text checks; image/gradient backgrounds receive visual review. The final build retains the production Stats API and stays within Cloudflare's asset limits.

Approved art, content, vivid faction/mode panels and all prior decisions are preserved. The rejected local development clip remains absent, its source files retained, with exactly three original click-to-play video cards. Shop is coming soon; beta has no fixed launch date. No DNS changes are needed.

Release mechanism: fast-forward push to the existing GitHub `main` branch triggers the established Cloudflare Pages build. Verify its successful check for the exact pushed commit, then run `SITE_URL=https://collapsedstargames.com node scripts/check-release.mjs` and the theme checks against the live domain. Git history and the Cloudflare check identify the deployed revision; do not infer deployment success from a push alone.
